{{!< default}}
<div class="page-tag">
    <h1>Tag: {{tag.name}}</h1>
    <ol class="posts">
        {{#foreach posts}}
            {{> post-list-item}}
        {{/foreach}}
    </ol>
    {{pagination}}
</div>